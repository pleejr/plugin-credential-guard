{
  "name": "plugin-credential-guard",
  "private": true,
  "type": "module",
  "description": "Tooling only. The plugin itself ships as TypeScript the engine strips at load; nothing here is bundled or published.",
  "scripts": {
    "typecheck": "tsc -p .",
    "test": "claude plugin test"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
